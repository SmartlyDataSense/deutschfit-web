"use client";

import { ProfilScreen } from "@/learner/profil/screens/ProfilScreen";

/**
 * Profil tab — S11 · Task 4. Replaces the placeholder wired in Task 1.3.
 * See `ProfilScreen.tsx` for composition (identity card, settings list,
 * account section, sign-out confirm dialog).
 */
export default function ProfilPage() {
  return <ProfilScreen />;
}
